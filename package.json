{
  "name": "github-aws-oidc",
  "version": "1.0.0",
  "description": "GitHub Actions AWS OIDC example with docker build and push to ecr",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
